#include "main.h"

/**
 * main - Function that prints the alphabet, in lowercase.
 * Return: 0
 */
void print_alphabet(void)
{
	char ch;

	char = 'a';

	while (char <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
}
